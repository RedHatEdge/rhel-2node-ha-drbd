---
# Option B — DRBD 9, two resources with opposite primaries.
#
#   ansible-playbook playbooks/20-option-b-drbd.yml -e storage_backend=drbd

- name: Storage layer — DRBD
  hosts: cluster
  become: true
  vars:
    storage_backend: drbd
  roles:
    - storage_drbd
    - guest_vms
